import "std";

fn main() {
    let anArray = @array(1, 2, 3);
    defer anArray.free();

    for &item; anArray {
        *item = 0;
    }

    for (item; anArray) @print("% ", item);
    @println("");
}